<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_movie_path(@movie), class: "btn btn-default pull-right")%>
  </div>
  <h2><%= @movie.title %></h2>
  <p><%= @movie.description %></p>
</div>
